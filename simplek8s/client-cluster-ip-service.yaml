apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web
  ports:
    # outside to allow
    - port: 3000
    # targeting container port
      targetPort: 3000